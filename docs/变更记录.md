# 变更记录

## 2026-09-17 · 第一关机关与跳跃细节

- 跳跃改为按下沿触发：保留土狼时间和落地前缓冲，加入松键短跳，消除长按落地自动连跳；失焦时清空输入，避免方向键卡住
- 落地加入角色压缩、尘环与轻微空中倾斜反馈
- 蒸汽改为安全余汽、预示漏汽、致命主汽柱三档粒子表现；新增带螺栓、盖缝发光和震颤的金属盖
- 压板改为脚底落地判定，加入实体下沉、半圈符文和 0.8s 双板咬合进度
- 门加入 `opening/open/closing` 状态、0.45s 升降动画、5s 门轴倒计时；角色停在门洞时延后闭合
- 新增跳跃、压板侧擦与门洞防夹回归测试；自动化现为 16 项

## 2026-09-17 · 移速与场景道具

- 烬移速 220→380、朔 200→350，跳跃略增
- 相机按 CSS 视口计算，避免分辨率放大把机关缩成看不见
- 熔岩/水/石台/压板/门/祭坛/残烬改为关卡坐标上的真实贴图，不再只用淡色方块
- 蒸汽柱加强预示；终点壁龛加发光框

## 2026-09-17 · 修复进入关卡黑屏

- React StrictMode 会对同一 canvas 初始化两次，且 `app.destroy(true)` 会把画布从 DOM 拆掉
- 资源改为先并行加载再创建渲染器；失败显示错误而不是空白
- 前景图抠绿失败会整屏盖住玩法层，已从绘制栈移除
- 进入关卡显示「裂隙开启中」加载态

## 2026-09-17 · Dokploy 部署文件

- 新增 `Dockerfile`、`Caddyfile`、`.dockerignore`、`deploy/dokploy-compose.yaml`
- `docs/部署运维.md` 改为可执行的 Dokploy 操作说明（端口 3000、`/healthz`、不发布宿主机端口）

## 2026-09-17 · 阶段 B 竖切

- 可运行 React + Pixi 壳：封面、说明、第 1 关、暂停/重开/结算
- 自研固定时间步：双人控制、元素伤害、寻热残烬、蒸汽周期、双压板 5s 闩、救援窗
- 角色由视频循环采帧（走/息/跳），绿幕抠图后写入 `public/assets/characters/`
- `npm test` 11 项通过，含第 1 关官方解法回放与「朔先冲残烬廊则死」
- 启动脚本 `scripts/dev.sh`、`scripts/start.sh` 已落地；Dockerfile 仍待阶段 E
- **当前限制：** 其余 17 关未做；残烬/蒸汽为逻辑+半透明叠层，不是独立粒子系统；抠绿边缘仍有暗绿溢色

## 2026-09-17 · Plan 阶段

- 新建项目 `frostfire-sanctum`（《霜焰双行》）
- 锁定：React + TypeScript + Vite 壳，PixiJS 8 渲染，自研 1/120s 物理，Dokploy + Caddy `/healthz`
- 锁定角色：焰行者「烬」、霜行者「朔」；18 关规则引入表；第 1 关完整官方解法
- 文档：设计、业务、关卡、开发、数据模型、接口、测试、部署、实现计划
- **未实现：** 游戏循环、素材、可玩关卡、启动脚本、Dockerfile
