# 测试文档

更新：2026-09-24。测试 / 实现维护者维护覆盖范围和限制。逻辑测试不需要浏览器，真实渲染使用浏览器另行验证。

## 自动化入口

```bash
npm test
npm run build
```

临时目录权限受限时可为命令显式设置 `TMPDIR` 到可写目录，不修改系统权限。当前共 13 个测试文件、117 项测试。

| 测试文件 | 覆盖 |
|---|---|
| hazards.test.ts | 水/熔岩/冰雾矩阵、蒸汽预示与致命相位 |
| jump.test.ts | 长按不连跳、松键短跳、落地缓冲 |
| wisp.test.ts | 最近目标、伤害、丢失回巢 |
| plates.test.ts | 单板/侧擦无效、双板 0.8s、闩锁 5s、关闭动画与门洞角色防夹 |
| level01-replay.test.ts | 单出口不清关、冒进与跳坑失败、蒸汽危险、按住门、官方零死亡解法 |
| later-levels.test.ts | 2–5 关零死亡解法、碰拨杆换潮、深潮淹没、内环冒进失败 |
| room-layout.test.ts | 五关完整出生点到出口输入回放，全部 6 件收集、0 死、不救援、三星；五种独立支路；第五关中室取符文、错过一轮后跳过可选支路仍能以 5/6 两星通关；安全出生点；高台不能绕过接应/反相门；三种舞台尺寸完整 fit；颜色拾取/去重/重开、符文开门前置、烧桥前置、齿轮循环、防夹、缺可选晶石评分 |
| runtime.test.ts | 暂停救援状态保留剩余时间、清除按住输入 |
| guidance.test.ts | 中室符文提示顺序、时序窗口/跨周期等待/防夹提示、蒸汽与油桥倒计时、深潮开门先降潮、救援优先级；查询不修改模拟 |
| movement.test.ts | 双角色左右跑速上限/一秒位移/慢行切换与释放、柔和加速/刹停、反向制动、半速精准移动、土狼跳、满跳高度/滞空/跨越距离、箱子与时钟隔离、撞墙清速、动画时钟/空中分段帧/落地恢复/顶点连续拉伸/倒地单次播放/减少动态 |
| interaction-variety.test.ts | 碎台预警/坍塌/恢复/暂停/重开/恢复防夹，持续压板不重复换相，倒地不能拨杆，交互标签与确认，递进三星目标和更短时序窗 |
| hazard-readability.test.ts | 多时刻蒸汽粒子不越出危险矩形、冷却余汽范围、阶段文字、寒气/热汽的分角色提示及伤害规则不变 |
| gate-presentation.test.ts | 五关门宽/中心/防绕关高度、框内双叶/横档裁剪、缓动端点、反向连续性、模拟钟暂停、重开与减少动态效果 |

回放只提交 Intent，不修改角色坐标、门、拾取状态。契约单元测试可显式摆放角色以隔离规则，不能把这类测试充当完整通关证明。

## 2026-09-22 浏览器验证

使用本地 Vite（5175）、Playwright Chromium。当前会话没有 Browser 插件，所以使用已安装的 Playwright；未安装新项目依赖。测试档在隔离浏览器上下文中临时解锁五关，不修改真实玩家存档。

- 1440×900：逐关载入、提示展开、暂停后时钟不变、继续、失焦自动暂停、键盘移动、Space 激活提示按钮、重开清零收集、返回选关。
- 1280×720、390×844、844×390：完整舞台、页面无横向溢出、按钮可访问。相机 fit 另有数值断言。
- 页面标题、非空内容、无 Vite 错误覆盖、控制台/页面异常、HUD 与舞台边界均检查。
- 保存五关、选关和响应式截图用于复核；截图文件不是构建依赖，也不入 Git。
- 1280×720 串联回归：在隔离页面接入现有输入回放策略，保留真实 Runtime 固定步进、渲染和存档，依次通关 1→5；每关显示三星 / 6 件收集物，前四关“下一关”有效，第五关无该按钮，回选关后五关成绩保留。不是声称人工双手玩通。
- 已确认 Pixi ticker 未自启动，避免与 Runtime 的手动绘制形成双循环。初测在软件渲染环境中第三关到 12.7s 模拟时间时触发 70s 测试等待超时；移除重复绘制并延长合理的测试等待窗口后，串联回归通过。未用这一结果宣称真实设备帧率。
- 第二轮第 5 关使用 Playwright 的真实键盘事件（非 demo），从出生点移动烬触发齿轮、留在接应板；朔在首闸外故意错过开门，等后续周期再跳入中室取符文，通过末闸并双人到出口。结果 0 死、5/6 收集、两星；未取左侧可选晶石，未修改模拟坐标或拾取状态。对应路线另由 `InputMap` 输入回归固定下来。
- 第二轮响应式检查发现 844×390 下目标栏与舞台顶部重叠 4px；横屏舞台上边距由 80px 改为 88px，不通过放宽断言掩盖重叠。
- 修正后再次验证 1280×720、390×844、844×390：无横向溢出，舞台不与目标栏/底栏重叠。再次逐关运行真实 Runtime + 输入回放，五关均为 0 死、6/6、三星，控制台及页面异常列表为空。第二轮截图与结果位于本机临时 QA 目录，不作为源码或构建依赖。

**视觉验收限制**：当前会话的图像查看工具拒绝图像输入，因此虽已生成真实截图、验证 DOM 和布局几何，不能宣称完成逐像素人工视觉签核或对概念图的 10/10 还原。未使用图像生成，也未复制外部位图。

## 操作、动作与危险区优化回归

2026-09-22，仍使用 Playwright Chromium（Browser plugin not available），本地地址 `http://127.0.0.1:5175`。旧开发进程已停止，确认端口空闲后重新使用项目 `scripts/dev.sh` 启动；没有占用/终止其他项目的 5173。

- 当时的真实键盘按 D 与 S+D：稳定速度分别为 360 与 180px/s；松键后回到零速度。这是降速前的历史记录，现行参数与复验见下节。不能把浏览器发送事件的时间误差当作精确物理基准，精确位移/刹车距离由单元测试覆盖。
- 真实键盘短按/长按 ↑：在净空足够的地面分别观察到约 90.21 / 111px 上升。初测在第五关出生点正下方跳跃，两种按法都受头顶平台限制，未达到差值断言；改为通过真实方向键走到净空区域后重测，不改坐标、不放宽断言。
- 第五关收紧窗口后再次通过非 demo 键盘流程：错过一期、等待后续首闸、中室取符文、末闸穿越、双出口，0 死、5/6、两星。
- 五关均再次在真实 Runtime 中以输入回放通过，0 死、6/6、三星。动作观察记录包含起跳/下落/落地/刹车；第二关观察到 push，第二/三关观察到 interact；第三/四关观察到 stable → cracking → gone。
- 1280×720、390×844、844×390 无横向溢出，舞台与目标栏/底栏不重叠。角色操作提示增加后未挤占舞台。
- 第一关分别等待真实冷却、蓄压、喷发阶段，再暂停观察：Pixi 标签对应“冷却 · 朔可过”“蓄压 · 朔退后”“喷发 · 朔禁入”，两处寒气区始终标“寒气 · 烬禁入”。为截图暂时隐藏暂停遮罩，不修改模拟阶段。
- 页面身份、非空内容、Vite 错误覆盖与控制台/页面异常检查通过。截图和 `motion-results.json` 保存于仓库外 `/home/miles/frostfire-qa`，不提交 QA 脚本/图片；`steam-safe.png`、`steam-telegraph.png`、`steam-lethal.png` 为三阶段证据。图像查看仍被工具拒绝，尚无人工视觉签核。
- 第三关烧桥完成后，浏览器确认显示“协作出口 · 等待双压板”；没有喷口的关卡不再显示不存在的蒸汽倒计时。
- TypeScript/Vite 构建通过；引入关内 Pixi 文字标识后主入口约 638.48kB（gzip 202.48kB），仍触发 >500kB 告警。未把通过构建或软件渲染回放等同于真实 GPU 帧率达标。

## 默认跑速下调复验

2026-09-22，基础跑速由烬 360 / 朔 350 调整为 300 / 290px/s；本节是当时验收记录，2026-09-23 的现行参数见下一节。

- `tests/movement.test.ts` 新增四组双角色/双方向用例，固定验证跑速上限、一秒位移、按下/释放精准慢行以及普通地面松键后小于 4px 的刹停距离。新增速度断言在旧参数下失败，修改后通过。
- `TMPDIR="$PWD/.tmp" npm test`：12 个文件、102 项通过；`.tmp` 为 Git 已忽略的可写临时目录。`npm run build` 通过，主入口仍约 638.48kB，保留 >500kB 告警。
- 初试 280 / 270px/s 时第一关回放无法保持完整可达；最终选择约 17% 的降速幅度，并把输入回放缺口前探由 44px 改为 20px，使低顶棚断口更靠边起跳。没有更改地形、跳高或测试通关条件。五关全收集、零死亡、三星及第五关错过周期后两星通关用例全部保留并通过。
- Browser plugin not available，使用已有 Playwright Chromium；原 5175 开发进程已退出，确认端口空闲后用 `PORT=5175 HOST=127.0.0.1 ./scripts/dev.sh` 启动，根路径 HTTP 200。没有新增依赖或改动启动脚本。
- 真实键盘同时按 D / → 测得稳定速度 300 / 290px/s，加入 S / ↓ 后为 150 / 145px/s，释放后均停到零速度。第一关从出生点用真实 D + W 跨过上层低顶棚断口，落地并取得火侧路线晶石，零死亡；未开启 demo，未修改模拟坐标。
- 1280×720、390×844 均检查标题、非空页面、Canvas、无横向溢出、无 Vite 错误覆盖；控制台与页面异常为空。结果位于仓库外 `/home/miles/frostfire-qa/speed-results.json`，截图为 `speed-1280.png`、`speed-390.png`。图像查看工具仍拒绝图像输入，不宣称完成人工视觉签核。
- 本轮浏览器验证仅覆盖跑速/慢行/停步、第一关跳口和上述视口；五关完整通关由逻辑输入回放验证，未重新执行五关浏览器串联或真人双人试玩。未提交、推送或部署；无 URL、页面结构、元数据、索引策略变化，不新增 SEO 行为。

## 人物速度与动作进一步放缓复验

2026-09-23，当前普通跑速为烬 240 / 朔 232px/s，精准慢行为 120 / 116px/s。同步放缓起步、跳跃、跑步/待机帧和落地反馈，不改变关卡几何、机关时钟、箱子下落或评分门槛。

- 先运行新增/更新的手感断言，旧实现有 8 项失败，涉及速度、起步过快、0.6s 滞空、跑动帧率和跳跃形变突变；实现修改后通过。新增箱子重力和世界时钟隔离测试，避免共享重力造成旁路影响。
- 初次降速后第一/五关输入回放在折返梯撞到上方平台后卡住，未修改通关断言或地形掩盖问题；回放改为横向起跳前先短助跑至基础速度的 90%。实际按键接口与玩家规则未改变。
- `TMPDIR="$PWD/.tmp" npm test`：12 个文件、106 项通过，保留五关全收集、零死亡、三星，以及第五关错过周期后两星通关回归。`npm run build` 和 `git diff --check` 通过；主入口约 638.60kB（gzip 202.51kB），仍有既存 >500kB 告警。
- Browser plugin not available，使用已有 Playwright 1.63.0 / Chromium。确认 5175 空闲后以 `PORT=5175 HOST=127.0.0.1 ./scripts/dev.sh` 启动，本地根路径 HTTP 200；未新增依赖或修改启动脚本。
- 选关进入第五关，使用真实 D / → 验证普通速度 240 / 232，再用 S+D / ↓+→ 验证 120 / 116，松键均停至零。返回第一关，以真实 D+W 跨越上层低顶棚断口、拾取火侧路线晶石并安全落地，零死亡、未启用 demo、未修改模拟坐标。
- 在真实渲染调用中记录到 idle / walk / jump / fall / land 动作状态；精确帧节奏、跳跃拉伸连续性与落地恢复时长由逻辑测试验证，不用静态截图推断动画流畅度。
- 1280×720、390×844 下页面标题正确、有实际内容与 Canvas、无横向溢出或 Vite 错误覆盖；控制台错误和页面异常为空。Chromium 有 WebGL `ReadPixels` GPU stall 性能警告，不将截图测试当作真实 GPU 帧率达标证据。
- 结果与截图保存于仓库外 `/home/miles/frostfire-qa/motion-20260923/`；`/data/tmp` 不可写，复用既有 QA 目录，未更改主机权限。图像查看工具拒绝图像输入，因此只有截图采集和 DOM/状态验证，不宣称人工视觉签核。
- 本轮浏览器覆盖双角色移速/慢行/停步、第一关断口与桌面/竖屏；五关全路线由逻辑回放验证，尚未重新完成五关浏览器串联、真人双人手感或跨浏览器验收。
- 已同步操作、业务、设计、开发与变更记录。SEO 仅潜在间接影响：未修改 URL、HTML 元数据、页面结构或收录策略；不新增 SEO 行为，也未做线上抓取验证。未提交、推送或部署。

## 闸门收窄与跳跃姿态复验

2026-09-24：闸门收窄为 24px，门框内上下双向收回；空中姿态限制在 0–3 帧，顶点跨零速度同帧，落地最大压缩为 8%，离地不带入落地/推箱形变。

- 改实现前先执行门宽与动作回归：旧实现 8 项失败（五关宽度、旧下落帧、顶点切帧、离地残留压缩）。随后新增门叶/横档边界、缓动端点、反向、暂停、重开和减少动态效果断言。
- `TMPDIR="$PWD/.tmp" npm test`：13 文件、117 项通过；保留五关全收集、零死亡、三星，以及第五关错过周期后两星回放和关闭机关不可被高台绕过的回归。
- `npm run build` 通过；主入口约 639.61kB（gzip 203.00kB），仍保留既有 >500kB 告警。`git diff --check` 和 Markdown 本地文件链接检查通过。
- Browser plugin not available，复用已安装 Playwright / Chromium。原 5175 服务退出后，使用 `PORT=5175 HOST=127.0.0.1 ./scripts/dev.sh` 启动本项目；无新增依赖或启动脚本变更。Playwright 临时目录显式设到可写 QA 目录，不修改系统权限。
- 真实键盘 D+W 跨过第一关低顶棚断口，拾取火侧路线晶石并安全落地；零死亡、未启用 demo、未改角色坐标。采集空中渲染姿态，确认顶点/下落帧存在、没有 4/5 帧或空中循环。
- 浏览器内另以 `officialPolicy → PairIntent → stepSim` 加速执行五关回放，实际调用当前 Pixi 视图；五关均 6/6 收集、0 死亡、三星。采集终点门连续开启进度及各关打开状态截图，不通过改坐标、补收集或强制开门完成回放。加速回放不代表真人操作或 GPU 帧率验收。
- 1280×720、390×844 页面标题和实际内容正常，有 Canvas、无横向溢出、无 Vite 错误覆盖；控制台错误与页面异常为空。截图时 Chromium 出现 WebGL `ReadPixels` GPU stall 警告，不据此声明真实设备性能通过。
- 通过实际“暂停”按钮进入暂停后，用独立渲染夹具设置半闭合门，等待后进度仍为 0.5；模拟减少动态效果时直接显示逻辑开启，实际点击“重开”恢复关闭且无残留过渡。半闭合夹具仅证明显示层暂停/无动画契约，不冒充玩家触发的关门流程；碰撞防夹和反向连续性另由单元测试覆盖。
- 截图及脚本保存在仓库外 `/home/miles/frostfire-qa/gates-20260924/`：`results.json`、`before.png`、`after-closed.png`、`jump.png`、`after-1280.png`、`after-390.png`、`open-1.png` 至 `open-5.png`、`closing-paused.png`。图像查看工具不支持本次图像输入，只有截图采集与几何/DOM/渲染状态验证，未完成人工视觉签核。
- 本次不更改跳跃速度、重力、输入、门锁计时或存档；未执行真人双人手感、其他浏览器或生产验收。无 URL、HTML 元数据、索引策略变化，SEO 仅潜在渲染性能间接影响，未作线上抓取或 Core Web Vitals 结论。未提交、推送或部署。

## 真人试玩清单

1. 第一关朔攀三层梯、跳支路、返回底道；烬跨阁楼断口并诱敌；两人踩门板和等待蒸汽。
2. 第二关先取符文再推箱升潮，注意深水与中水区别；确认限时出口允许两人会合。
3. 第三关烬取阁楼符文，从高踏台跨井；朔先过桥再由烬点燃，不能忽略烧桥直接开门。
4. 第四关攀较高折返梯，经左悬台返回底道，再在内环切换闸门；第五关故意错过一次水闸，等下一周期进入中室，跳上石台取冰符文，左侧普通晶石可不取。
5. 双人到达各自出口才能结算，下一关正常进入，第五关无虚假下一关。
6. 失焦暂停；救援中反复暂停不延长救援；键盘 Tab/Enter/Space 可操作按钮。
7. 小屏不裁掉角色与出口；提示不常驻遮挡；系统减少动态效果不改变危险周期。
8. S / ↓ 与方向键组合能精准对板；起跳、顶点、下落不循环错帧；第三/四关碎台失败后可恢复重试。
9. 能在不查攻略的情况下区分“寒气伤烬”和“热汽伤朔”；热汽的冷却/蓄压/喷发边界与标识清晰，人物和落脚点不被雾遮挡。此项仍需真人视觉确认。

## 未覆盖风险

- 自动解法证明可达性，不代表真人手感、难度、平均用时或键盘硬件多键冲突已验收。
- 未测试 Safari / Firefox、实际手机键盘、触屏双人输入（未实现）、GPU 驱动兼容和长期资源泄漏。
- 存档完整校验、浏览器存储被拒绝、设置 UI、音效尚未解决。
- Vite 主入口包仍有 >500kB 构建告警；没有线上性能指标或 SEO 抓取验证。
- 本次未部署，Caddy/Dokploy/证书与外网健康不在此次验证范围。
